- 页面组件遵循无状态展示模式，直接从 `@/lib/data` 导入常量数据进行渲染，不引入复杂的状态管理库。
- API 路由采用 Next.js Route Handlers 规范，在 `route.ts` 中导出命名的 HTTP 方法函数（如 POST, GET）处理请求。
- 交互型组件（如表单）显式声明 "use client" 指令以启用 React Hooks，并与服务端组件在布局层级进行隔离。